<li class="list-group-item list-group-item-warning">
		{{name}}
	<br>
	<button class="change-devoured btn btn-warning" data-id="{{id}}" data-newdevoured="{{devoured}}">
		{{#if devoured}}Devour It!!{{else}}Reorder!!{{/if}}
	</button>
</li>